export interface RoomInterface {
    checkIn: string;
    checkOut: string;
    roomType: string;
    roomDescription: string;
    roomPrice: number;
    roomImages: [string];
    maxPeople: number;
}
